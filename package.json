{
  "name": "errgroup",
  "version": "0.1.0",
  "description": "Structured concurrency for Node.js inspired by Go's errgroup",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "go-like-ctx": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.0.16"
  }
}
